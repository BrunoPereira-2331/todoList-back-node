const tasks = require('../controllers/tasks')

module.exports = (app) => {

  // app.post('/tasks', tasks.create);
  
  // app.get('/tasks', tasks.findAll)

  // app.get('/tasks/:taskId', tasks.findById);

  // app.put('/tasks/:taskId', tasks.update);

  // app.delete('/tasks/:taskId', tasks.delete);

}