const history = require('../controllers/tasks')

module.exports = (app) => {

  // app.post('/history', history.create);
  
  // app.get('/history', history.findAll)

  // app.get('/history/:taskId', history.findById);

  // app.put('/history/:taskId', history.update);

  // app.delete('/history/:taskId', history.delete);

}